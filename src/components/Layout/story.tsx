import { FunctionalComponent, h } from 'preact';
import Layout from '.';

export default { title: 'Layout' }

export const LayoutStory: FunctionalComponent = () => (
    <Layout>
        Layout
    </Layout>
);